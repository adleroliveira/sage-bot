import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2",
      light: "#e3f2fd",
    },
    secondary: {
      main: "#9c27b0",
      light: "#f3e5f5",
    },
    background: {
      default: "#f5f5f5",
    },
  },
});

export default theme;
